name: Process AI Stems

on:
  push:
    branches:
      - main
    paths:
      - 'process_stems.py'
      - '.github/workflows/process-stems.yml'
      - 'playlists/*.json'
      - '*.mp3'
      - '*.wav'
      - '*.m4a'
      - '*.flac'
  workflow_dispatch:

jobs:
  process-stems:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -U yt-dlp
          pip install demucs

      - name: Run Stem Processor
        run: |
          python process_stems.py

      - name: Commit and Push Stems
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add samples/ playlists/
          git status
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Process AI stems for new repertoire songs [skip ci]"
            git push origin main
          fi
